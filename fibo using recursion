#include<stdio.h>
#include<conio.h>
int fib(int n )
{
if(n<=1){

return n ;
}
else 
{
return (fib(n-1)+fib(n-2));
}}

int main()
{
	int x = fib(10);
	printf("fibonacci series of the guven number is : %d",x);
	return 0;
}
